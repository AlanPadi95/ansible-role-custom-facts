---

  - hosts: localhost
    become: yes
    # pre_tasks:
      # - file:
      #     path: "roles/"
      #     state: directory
      # - copy:
      #     src: "../../ansible-role-custom-facts"
      #     dest: "roles/"
    roles:
      - ansible-role-custom-facts